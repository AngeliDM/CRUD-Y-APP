<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Tracker</title>
    <link rel="stylesheet" href="/style.css"/>
</head>
<body>
   <h1> Task Tracker</h1>
   <form action="/quotes" method="POST">
    <input type="text" placeholder="Name" name="name">
    <input type="text" placeholder="Task" name="quote">
    <button type="submit">Create a task</button>
  </form> 

  <h2> Tasks to be completed  </h2>

<ul class="quotes">
  <!-- Loop through quotes -->
  <% for(var i = 0; i < quotes.length; i++) {%>
    <li class="quote">
      <!-- Output name from the iterated quote object -->
      <span><%= quotes[i].name %></span>:
      <!-- Output quote from the iterated quote object -->
      <span><%= quotes[i].quote %></span>
    </li>
  <% } %>
</ul>
<div>
    <h2>Add a random name and task</h2>
    <p>Add a random name and task to the board</p>
    <button id="update-button">Add</button>
</div>

<div>
    <h2>Remove a task</h2>
    <p>
      Delete a random name and task from the board
    </p>
    <button id="delete-button">Delete a task</button>
  </div>
  <div id="message"></div>
</body>
<script src="/main.js"></script>
</html>